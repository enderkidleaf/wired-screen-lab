# 2026-09-13 进度汇总

## 已完成

Windows 界面整合管理员授权、虚拟扩展屏创建和 USB 连接；取消授权不再误报驱动未安装。Android 0.3.1 控制面板可隐藏，保留恢复入口，支持详情折叠、保持亮屏与偏好记忆，并显示断线状态。

修正 Android 首帧初始化、原生 MFT 事件额度与缓冲对齐；补充离线回归。四帧发送队列增加短暂背压，保留顺序且不丢参考帧；增加发送阶段计时。

## 已验证与限制

- 小米 15S Pro 兼容动态扩展屏：20 秒、1183 帧，预热后平均解码 59.93 fps，正常停止。
- 原生路径：初始化修正后 248 帧过载停止，约 27–36 fps；未解决持续吞吐回退。
- NVENC 测试画面分段测量：包就绪到发送中位 0.0434 ms、socket 写入中位 0.1907 ms、包就绪到回调确认中位 19.63 ms。不是捕获到亮屏延迟。
- 合并写入未呈现收益，已撤回；此前不等负载的码流对照不能证明 USB 无瓶颈。
- 手机面板已构建安装并确认隐藏状态；完整操作与 Tab S4 布局仍待确认。

## 本地目录整理

用户的运行目录现在只保留最新 EXE、APK、必需 DLL/工具和说明。旧程序、早期网页版、源码、构建工具和测试记录移入“历史归档/2026-09-13整理前”，已做文件 SHA256 和程序自检。源码仓库仍保留标准开发目录结构，未将本机 SDK、二进制依赖、桌面录像、原始日志或整个归档上传。构建入口仍为 native/scripts/build.py。

## 下一步

1. 完成手机控制面板实际操作与小屏布局验收。
2. 补齐接收线程退出/重连生命周期管理。
3. 固定同一负载和输入样本，定位原生编码路径吞吐与积压。
4. 长时间 1080p60、亮屏延迟、Tab S4 和驱动分发验收。
