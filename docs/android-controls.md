# Android 接收端界面 0.3.0

- 半透明圆角控制面板，连接提示与技术统计分开显示。
- 点击“隐藏面板”收起状态信息；右上角“控制”恢复面板。
- 隐藏状态与保持亮屏设置保存在 App 内，重开后保留。
- “详细信息”展开帧率、解码器和低延迟模式；面板可滚动，便于小屏和大字体使用。
- “保持亮屏”默认开启；关闭后按系统超时息屏，息屏可能暂停连接。
- 断开时清除旧统计。视频协议、编码和解码参数未调整。

2026-09-12：完整构建通过，安装到小米 15S Pro 成功。截图与 UI 层级确认隐藏后只显示“控制”入口，重启保持隐藏。因设备限制 INJECT_EVENTS，无法自动点击；展开/收起和亮屏开关的完整操作等待用户确认。尚未验证 Tab S4 布局。

## 0.3.1（2026-09-13）

隐藏面板时，未连接入口显示为暖色“未连接”，收到连接统计后恢复“控制”；不强制展开面板。控件改为原生按钮并增加水波纹点击反馈，改善可访问性；面板宽度按实际窗口限制，保留滚动。完整构建、初始化离线回归及手机安装通过。设备息屏，新增交互尚未完成目视验收。未修改传输或解码参数。
