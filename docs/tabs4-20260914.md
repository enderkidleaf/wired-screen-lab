# Tab S4 默认路径修正

2026-09-14：SM-T837V 已安装 Android 0.3.1。强制兼容动态扩展屏 30 秒通过，1758 帧，预热后平均解码 60.00 fps。

随后用户界面两次失败日志 142040/142117 显示 auto 优先选择 d3d11-qsv，而非通过测试的 NVENC。约 1 MB 关键帧后 socket 写入阻塞约 0.5 秒，触发队列过载；这不是原生 native-mf 路径。

修正 UI auto 和 CLI 默认走兼容编码器选择，GPU QSV 仅在 UI 明确选择 h264_qsv 或 CLI --gpu 时启用；--compat 优先。没有加大队列或放宽等待。原生路径仍是实验选项。

默认 CLI（无 --compat）30 秒验证实际选择 NVENC，1719 帧正常结束，无队列过载；预热后平均解码 60.33 fps、呈现回调 60.37 fps。共享通道心跳往返平均 148.86 ms，仍存在明显排队/延迟，不能认为低延迟验收通过。日志 usb-20260914-142346.jsonl。此测试验证默认策略，UI 点击流程尚需用户复核。

根目录 WiredScreen.exe 已替换，旧版保留于本地历史归档/2026-09-14默认路径修正前。用户使用 auto 即可。
