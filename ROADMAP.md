# 当前目标：低延迟原生 USB 扩展副屏

下一阶段以 [低延迟实施方案](docs/low-latency-plan.md) 为准：P0 测量和帧正确性 → P1 参数与接收队列 → P2 原生 GPU 编码 → P3 IDD 纹理直连 → P4 设备验收。目标是接近物理副屏的响应和稳定性，具体门槛属于项目验收目标，尚未达成。

Windows 向安卓提供单块 1920×1080、60 Hz 扩展桌面。小米 15S Pro 为当前测试设备，最终在三星 Tab S4 验收。

## 2026-09-10 实测进度

- 已完成：本机测试签名驱动安装、单块虚拟显示器枚举和扩展桌面。
- 已完成：按虚拟屏名称定位 DXGI 显卡及输出，传输真实扩展桌面到手机；用户确认看到画面。
- 短测：30 秒，发送 1750 帧、发送端丢弃过期帧 10 帧。去掉前 5 次采样后，平均解码 59.98 fps、呈现回调 59.94 fps、USB 往返 6.93 ms，往返最大 47.56 ms。
- 证据：native/dist/logs/usb-20260910-190118.jsonl、artifacts/usb-virtual-test.txt。

## 尚待验收

2026-09-11 驱动连接：新增本地管理员认证、服务进程核对、未命名纹理句柄交接、只读元数据映射和独立连接线程。已接入可选 IDD 构建，WDK 编译通过；独立进程管理员 1080p 交接、非管理员拒绝、错误 PID 拒绝及断连检查通过。实验驱动已签名安装，真实 IDD 的 1920×1080 三帧（序号 71、72、73）直接交给独立进程验证通过；测试虚拟屏已移除。手机编码器尚未消费共享纹理。详见 [交接进度](docs/shared-texture-handoff.md)。

2026-09-11 纹理直连准备：新增三槽原生 D3D11 共享纹理池，跨进程 1080p 最新帧及逐像素验证通过，满池、正常退出、持锁崩溃检查通过。详见 [共享纹理交接进度](docs/shared-texture-handoff.md)。目前为独立组件，尚未接入 IDD 或编码器，不改变手机正在使用的链路。

2026-09-11 GPU 路径：虚拟屏自动模式优先尝试 D3D11 → QSV 映射、GPU 色彩转换和 H.264 编码，移除了该路径中显式的 CPU 像素下载与重新上传。启动时先用真实捕获输出验证三帧，失败则恢复兼容路径；`--virtual --compat` 可强制兼容模式。会话日志记录 `pixelPath`。本机虚拟屏（adapter=0、output=1）三帧编码、提取及解码通过，脚本为 `native/scripts/test_gpu_packets.ps1`，本地证据为 `artifacts/gpu-roundtrip-test.txt`。仍使用 FFmpeg 桌面复制，不等同于 IDD 纹理直连或完整原生编码器；尚未验证手机上的延迟收益和长期稳定性。

逐包输出阶段：发送主线已从 AUD 分隔解析改为读取流式 AVI 中的编码包长度，完成一个包即可发送，不再等待下一帧 AUD。AVI 只用于电脑内部封装，手机仍接收 H.264 包。单帧不读下一字节的回归检查、NVENC 非可寻址管道的三包提取及解码检查通过（`native/scripts/test_encoded_packets.ps1`）。这不是新建原生 GPU 编码器；仍使用 FFmpeg，GPU→CPU 搬运和端到端延迟验证待完成。遵照用户要求没有再进行长时间手机测试。

2026-09-11：已完成 [VBV 实机对照](docs/vbv-results-20260911.md)，12 次有效测试加 1 次启动过载失败。小缓冲未呈现一致改善，保留默认参数；下一步定位启动帧突发和回调排队，继续原生编码及捕获计时。

P1 参数修正：VBV 容量按 bitrate × frames / 60 计算，虚拟屏命令行已接入参数，并恢复非默认显卡的捕获选择。每次会话记录实际编码参数。`native/scripts/test_vbv.ps1` 可在管理员 PowerShell 下执行默认/1/2/4 帧各三轮动态对照；目前手机未连接，尚未运行该对照，不能宣布延迟改善。

1. 连续至少 5 分钟动态内容，统计帧率分布、卡顿和解码错误；不能以短测平均值宣布稳定 60 fps。
2. 独立测量端到端显示延迟，继续减少捕获、编码、解码缓冲。
3. 手机接收端重复连接稳定性，以及不同显卡布局的自动选择。
4. 三星 Tab S4 的性能、发热、拔插恢复测试。
5. 正式驱动签名、安装体验和产品化。

当前开发版仍使用桌面复制捕获虚拟屏，再编码经 USB 传输；直接从 IDD 纹理送入编码器尚未实现。
