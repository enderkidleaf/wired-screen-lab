## 2026-09-10 最新实测

虚拟设备注册和 Windows 扩展桌面测试已通过。原生回调桥接层修复了 C# 回调导致的 0x8007007E；直接调用显示配置接口后，三块虚拟屏成功加入桌面。当前沿用微软示例默认模式，尚未收敛为单块 1920×1080 60 Hz，也未验证虚拟屏通过 USB 到手机的完整链路。下方未注册副屏的描述为历史进度。

管理员 Windows PowerShell 运行 native/scripts/test_virtual_display.ps1 -Extend 可复测，结束自动移除设备。实测日志在 artifacts/idd-extend-probe.txt。原生构建和协议自检通过。

# 当前目标：原生 USB 扩展副屏

## 进度声明

当前版本**未实现副屏/虚拟扩展屏功能**。已实现的范围是“有线读取屏幕内容并投屏”：PC 读取已有屏幕画面，通过 ADB USB 直连传输 H.264，安卓 App 解码显示。该功能不会向 Windows 注册新的显示器。

2026-09-08 更新：开发 Windows PC 程序 + 安卓 APK，通过 USB 直连实现 1920×1080、60 fps 扩展副屏。不依赖 Wi-Fi、USB 网络共享或浏览器 WebRTC。先在小米 15S Pro 验证，再在三星 Tab S4 验收。

新顺序：原生端与 ADB USB 通道 → H.264/MediaCodec 视频 → 低延迟优化 → Windows IDD 扩展屏 → Tab S4 验收 → AOA 产品化。

ADB 版本中本机 socket 仅作为进程接口；跨设备数据由 ADB USB 搬运，强制选择 USB 设备（adb -d）。

应用目标条目仍是旧的暂停目标，目标工具不支持覆盖未完成目标。下方为旧网页原型的历史记录，不代表原生版已经完成。

## 最终目标

Windows 新增一个真正的扩展显示器，通过 USB 向三星 Tab S4 传送 1920×1080、60 fps 画面。小米 15S Pro 为当前可用测试设备。
每个阶段保留真实测试证据，不以“请求 60 fps”代替“实测 60 fps”。

| 阶段 | 验收 | 状态 |
|---|---|---|
| 1. 传输原型 | 配对、1080p 视频收发、帧率记录、会话停止 | 已实现，本机回环通过 |
| 2. USB 手机测试 | 小米关闭 Wi-Fi，通过 USB 网卡接收；拔线中断 | 待连接实机 |
| 3. 性能定位 | 分别测源帧率、编码、接收解码；连续动态测量 | H.264 本机约 30 秒平均 60.1 fps；持续/实机验收待完成 |
| 4. Windows 扩展屏 | 安装 IDD 后系统识别第二屏、可拖窗口过去 | 进行中：PC 程序已可创建 IDD 软件设备；WDK 28000 已构建 DLL/INF/CAT 驱动包，并已准备本机测试签名脚本；待开启测试模式、重启、安装和实机验收 |
| 5. Tab S4 验收 | 1080p60 持续测试、发热、拔插恢复 | 待设备 |
| 6. 产品完善 | 便捷安装、自动重连、触控 | 后续 |

## 当前环境

- Node.js 24、本机 Edge、Visual Studio Build Tools 已找到。
- 已验证 VS 2026 + WDK 28000 项目本地工具链可构建 IDD 示例驱动；构建产物尚未签名或安装，`native/scripts/sign_install_test_idd.ps1` 将用于本机测试签名。
- 原生 ADB USB 通道不需要 USB 网络共享；浏览器原型才需要它。
- 尚未安装任何系统显示驱动。

## 下一次继续

开启 Windows 测试签名模式并重启，再为驱动包创建受信任的测试签名并安装。之后在 Windows 枚举 IDD 显示器，并验证现有 ADB USB 视频链路能否捕获并发送该显示器。未完成真实 1080p60 测量前，不宣布扩展副屏完成。

2026-09-10 实机更新：已确认 Windows 测试签名模式生效，驱动已通过本机测试证书签名并加入驱动库（oem54.inf）。修正了 PC 端系统接口 DLL 引用和签名证书存储选择。注册设备仍返回 HRESULT 0x8007007E，尚未枚举出虚拟显示器；副屏和 1080p60 验收仍未完成。原生构建及协议自检通过。
